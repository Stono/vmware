'use strict';
var VCloud = require('../lib/vmware').VCloud;

describe('vcloud', function() {

  it('Should create an instance of the vcloud client');
  it('Should login to the web service');
  it('Should return an error when login credentials are incorrect');
  it('Should return a list of the supported versions');

});
